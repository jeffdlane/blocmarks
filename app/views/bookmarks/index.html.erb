<h1>Bookmarks#index</h1>
<p>Here's a list of all Blocmarks</p>
<% @bookmarks.each do |bookmark| %>
  <p><%= link_to bookmark.title, "#{bookmark.url}" %></p>
<% end %>

<container>
  <h3>Create a new bookmark</h3>
    <%= render partial: "form", locals: {bookmark: @bookmark} %>
</container>